Use: eas update

To see it in action, start the Expo development server. Run the commands below in a terminal opened at the project.

PowerShell (Windows):
```powershell
cd C:\Users\redst\Desktop\gitHub\personal\HabitForge\frontend\HabitForgeApp
npm start
```

If you're using Git Bash, WSL, macOS or Linux, use a bash-style path:
```bash
cd /c/Users/redst/Desktop/gitHub/personal/HabitForge/frontend/HabitForgeApp
npm start
```

This will start the Metro Bundler and provide a QR code. Scan the QR with the Expo Go app on an Android or iOS device, or run the
app in a simulator or web browser.

The login and sign-up buttons will now use Firebase to authenticate users.

What would you like to work on next? We can proceed with:
1. Adding Google OAuth to the login screen.
2. Setting up a dashboard screen to redirect to after login.
3. Beginning the User Profile Creation feature.